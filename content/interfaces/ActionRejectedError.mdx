---
title: ActionRejectedError
description: This Error indicates a request was rejected by the user.   In most
  clients (such as MetaMask), when an operation requires user  authorization
  (such as ...
---

This Error indicates a request was rejected by the user.

 In most clients (such as MetaMask), when an operation requires user
 authorization (such as `signer.sendTransaction`), the client
 presents a dialog box to the user. If the user denies the request
 this error is thrown.

## Extends

- [`quaisError`](/content/interfaces/quaisError.mdx)\<`"ACTION_REJECTED"`\>

## Properties

| Property | Type | Description | Inherited from |
| :------ | :------ | :------ | :------ |
| `action` |  \| `"unknown"` \| `"sendTransaction"` \| `"signTransaction"` \| `"signMessage"` \| `"signTypedData"` \| `"requestAccess"` | The requested action. | - |
| `code` | [`ErrorCode`](/content/type-aliases/ErrorCode.mdx) | The string error code. | [`quaisError`](/content/interfaces/quaisError.mdx).`code` |
| `error?` | `Error` | Any related error. | [`quaisError`](/content/interfaces/quaisError.mdx).`error` |
| `info?` | `Record`\<`string`, `any`\> | Additional info regarding the error that may be useful.<br /><br /> This is generally helpful mostly for human-based debugging. | [`quaisError`](/content/interfaces/quaisError.mdx).`info` |
| `reason` | `"rejected"` \| `"pending"` \| `"expired"` | The reason the action was rejected.<br /><br /> If there is already a pending request, some clients may indicate<br /> there is already a `"pending"` action. This prevents an app<br /> from spamming the user. | - |
| `shortMessage` | `string` | A short message describing the error, with minimal additional<br /> details. | [`quaisError`](/content/interfaces/quaisError.mdx).`shortMessage` |
