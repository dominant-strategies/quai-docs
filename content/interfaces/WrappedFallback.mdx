---
title: WrappedFallback
---

A Fallback or Receive function on a Contract.

```ts
interface WrappedFallback(overrides?): Promise<ContractTransactionResponse>
```

## Parameters

| Parameter | Type |
| :------ | :------ |
| `overrides`? | `Omit`\<[`TransactionRequest`](/content/type-aliases/TransactionRequest), `"to"`\> |

## Returns

`Promise`\<[`ContractTransactionResponse`](/content/classes/ContractTransactionResponse)\>

## Source

[src/contract/types.ts:250](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/contract/types.ts#L250)

## Methods

### estimateGas()

```ts
estimateGas(overrides?): Promise<bigint>
```

Estimate the gas to send a transaction to the contract fallback.

For non-receive fallback, `data` may be overridden.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `overrides`? | `Omit`\<[`TransactionRequest`](/content/type-aliases/TransactionRequest), `"to"`\> | The transaction overrides. |

#### Returns

`Promise`\<`bigint`\>

A promise resolving to the estimated gas.

#### Source

[src/contract/types.ts:294](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/contract/types.ts#L294)

***

### populateTransaction()

```ts
populateTransaction(overrides?): Promise<ContractTransaction>
```

Returns a populated transaction that can be used to perform the fallback method.

For non-receive fallback, `data` may be overridden.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `overrides`? | `Omit`\<[`TransactionRequest`](/content/type-aliases/TransactionRequest), `"to"`\> | The transaction overrides. |

#### Returns

`Promise`\<[`ContractTransaction`](/content/interfaces/ContractTransaction)\>

A promise resolving to the populated transaction.

#### Source

[src/contract/types.ts:261](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/contract/types.ts#L261)

***

### send()

```ts
send(overrides?): Promise<ContractTransactionResponse>
```

Send a transaction to the contract fallback.

For non-receive fallback, `data` may be overridden.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `overrides`? | `Omit`\<[`TransactionRequest`](/content/type-aliases/TransactionRequest), `"to"`\> | The transaction overrides. |

#### Returns

`Promise`\<[`ContractTransactionResponse`](/content/classes/ContractTransactionResponse)\>

A promise resolving to the transaction response.

#### Source

[src/contract/types.ts:283](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/contract/types.ts#L283)

***

### staticCall()

```ts
staticCall(overrides?): Promise<string>
```

Call the contract fallback and return the result.

For non-receive fallback, `data` may be overridden.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `overrides`? | `Omit`\<[`TransactionRequest`](/content/type-aliases/TransactionRequest), `"to"`\> | The transaction overrides. |

#### Returns

`Promise`\<`string`\>

A promise resolving to the transaction response.

#### Source

[src/contract/types.ts:272](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/contract/types.ts#L272)
