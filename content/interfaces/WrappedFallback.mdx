[**quais6**](../README.md) • **Docs**

***

[quais6](../globals.md) / WrappedFallback

# Interface: WrappedFallback()

A Fallback or Receive function on a Contract.

> **WrappedFallback**(`overrides`?): `Promise`\<[`ContractTransactionResponse`](../classes/ContractTransactionResponse.md)\>

## Parameters

• **overrides?**: `Omit`\<[`TransactionRequest`](../type-aliases/TransactionRequest.md), `"to"`\>

## Returns

`Promise`\<[`ContractTransactionResponse`](../classes/ContractTransactionResponse.md)\>

## Source

[src.ts/contract/types.ts:256](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/types.ts#L256)

## Methods

### estimateGas()

> **estimateGas**(`overrides`?): `Promise`\<`bigint`\>

Estimate the gas to send a transaction to the contract fallback.

 For non-receive fallback, `data` may be overridden.

#### Parameters

• **overrides?**: `Omit`\<[`TransactionRequest`](../type-aliases/TransactionRequest.md), `"to"`\>

The transaction overrides.

#### Returns

`Promise`\<`bigint`\>

A promise resolving to the estimated gas.

#### Source

[src.ts/contract/types.ts:297](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/types.ts#L297)

***

### populateTransaction()

> **populateTransaction**(`overrides`?): `Promise`\<[`ContractTransaction`](ContractTransaction.md)\>

Returns a populated transaction that can be used to perform the
 fallback method.

 For non-receive fallback, `data` may be overridden.

#### Parameters

• **overrides?**: `Omit`\<[`TransactionRequest`](../type-aliases/TransactionRequest.md), `"to"`\>

The transaction overrides.

#### Returns

`Promise`\<[`ContractTransaction`](ContractTransaction.md)\>

A promise resolving to the populated transaction.

#### Source

[src.ts/contract/types.ts:267](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/types.ts#L267)

***

### send()

> **send**(`overrides`?): `Promise`\<[`ContractTransactionResponse`](../classes/ContractTransactionResponse.md)\>

Send a transaction to the contract fallback.

 For non-receive fallback, `data` may be overridden.

#### Parameters

• **overrides?**: `Omit`\<[`TransactionRequest`](../type-aliases/TransactionRequest.md), `"to"`\>

The transaction overrides.

#### Returns

`Promise`\<[`ContractTransactionResponse`](../classes/ContractTransactionResponse.md)\>

A promise resolving to the transaction response.

#### Source

[src.ts/contract/types.ts:287](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/types.ts#L287)

***

### staticCall()

> **staticCall**(`overrides`?): `Promise`\<`string`\>

Call the contract fallback and return the result.

 For non-receive fallback, `data` may be overridden.

#### Parameters

• **overrides?**: `Omit`\<[`TransactionRequest`](../type-aliases/TransactionRequest.md), `"to"`\>

The transaction overrides.

#### Returns

`Promise`\<`string`\>

A promise resolving to the transaction response.

#### Source

[src.ts/contract/types.ts:277](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/types.ts#L277)
