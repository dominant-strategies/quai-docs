---
title: FetchGetUrlFunc
---

```ts
type FetchGetUrlFunc: (req, signal?) => Promise<GetUrlResponse>;
```

Used to perform a fetch; use this to override the underlying network fetch layer. In NodeJS, the default uses the
"http" and "https" libraries and in the browser `fetch` is used. If you wish to use Axios, this is how you would
register it.

## Parameters

| Parameter | Type |
| :------ | :------ |
| `req` | [`FetchRequest`](/content/classes/FetchRequest) |
| `signal`? | `FetchCancelSignal` |

## Returns

`Promise`\<[`GetUrlResponse`](/content/type-aliases/GetUrlResponse)\>

## Source

[utils/fetch.ts:82](https://github.com/dominant-strategies/quais.js/blob/7ad0a969a30d30fc2cf79fa0dcda980927cb6fd5/src/utils/fetch.ts#L82)
