---
title: PerformActionRequest
---

```ts
type PerformActionRequest: 
  | {
  "method": "broadcastTransaction";
  "shard": string;
  "signedTransaction": string;
 }
  | {
  "blockTag": BlockTag;
  "method": "call";
  "shard": string;
  "transaction": PerformActionTransaction;
 }
  | {
  "method": "chainId";
  "shard": string;
 }
  | {
  "method": "estimateGas";
  "shard": string;
  "transaction": PerformActionTransaction;
 }
  | {
  "address": string;
  "blockTag": BlockTag;
  "method": "getBalance";
  "shard": string;
 }
  | {
  "address": string;
  "method": "getOutpointsByAddress";
  "shard": string;
 }
  | {
  "blockTag": BlockTag;
  "includeTransactions": boolean;
  "method": "getBlock";
  "shard": string;
 }
  | {
  "blockHash": string;
  "includeTransactions": boolean;
  "method": "getBlock";
  "shard": string;
 }
  | {
  "method": "getBlockNumber";
  "shard": string;
 }
  | {
  "address": string;
  "blockTag": BlockTag;
  "method": "getCode";
  "shard": string;
 }
  | {
  "method": "getGasPrice";
  "shard": string;
  "txType": boolean;
 }
  | {
  "filter": PerformActionFilter;
  "method": "getLogs";
  "shard": string;
 }
  | {
  "method": "getMaxPriorityFeePerGas";
  "shard": string;
 }
  | {
  "address": string;
  "blockTag": BlockTag;
  "method": "getStorage";
  "position": bigint;
  "shard": string;
 }
  | {
  "hash": string;
  "method": "getTransaction";
  "shard": string;
 }
  | {
  "address": string;
  "blockTag": BlockTag;
  "method": "getTransactionCount";
  "shard": string;
 }
  | {
  "hash": string;
  "method": "getTransactionReceipt";
  "shard": string;
 }
  | {
  "hash": string;
  "method": "getTransactionResult";
  "shard": string;
 }
  | {
  "method": "getRunningLocations";
  "shard": string;
 }
  | {
  "method": "getProtocolTrieExpansionCount";
  "shard": string;
 }
  | {
  "amt": number;
  "blockTag": BlockTag;
  "method": "getQiRateAtBlock";
  "shard": string;
 }
  | {
  "amt": number;
  "blockTag": BlockTag;
  "method": "getQuaiRateAtBlock";
  "shard": string;
 }
  | {
  "method": "getProtocolExpansionNumber";
 }
  | {
  "method": "getPendingHeader";
};
```

The [**AbstractProvider**](/content/classes/AbstractProvider) methods will normalize all values and pass this type to
[**AbstractProvider._perform**](/content/classes/AbstractProvider#_perform).

## Source

[src/providers/abstract-provider.ts:422](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/abstract-provider.ts#L422)
