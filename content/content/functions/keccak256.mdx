---
title: keccak256
---

```ts
function keccak256(_data): string
```

Compute the cryptographic KECCAK256 hash of `data`.

The `data` **must** be a data representation, to compute the hash of UTF-8 data use the [**id**}(../functions/id)
function.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `_data` | [`BytesLike`](/content/type-aliases/BytesLike) | The data to hash. |

## Returns

`string`

DataHexstring

The hash of the data.

## Example

```ts
keccak256('0x');
//_result:

keccak256('0x1337');
//_result:

keccak256(new Uint8Array([0x13, 0x37]));
//_result:

// Strings are assumed to be DataHexString, otherwise it will
// throw. To hash UTF-8 data, see the note above.
keccak256('Hello World');
//_error:
```

## Source

[crypto/keccak.ts:49](https://github.com/dominant-strategies/quais.js/blob/751386e3ad6372322a3c76548f56b903bfed1896/src/crypto/keccak.ts#L49)
