---
title: parseUnits
description: ""
---

```ts
function parseUnits(value, unit?): bigint
```

Converts the //decimal string// `value` to a BigInt, assuming
 `unit` decimal places. The `unit` may the number of decimal places
 or the name of a unit (e.g. `"gwei"` for 9 decimal places).

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `value` | `string` | The value to convert. |
| `unit`? | `string` \| [`Numeric`](/content/type-aliases/Numeric.mdx) | The unit to convert from. |

## Returns

`bigint`

The converted value.

## Throws

If the unit is invalid.

## Throws

If the value is not a string.

## Source

[src.ts/utils/units.ts:77](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/utils/units.ts#L77)
