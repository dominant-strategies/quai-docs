---
title: assertArgument
description: ""
---

```ts
function assertArgument(
   check, 
   message, 
   name, 
   value): asserts check
```

A simple helper to simply ensuring provided arguments match expected
 constraints, throwing if not.

 In TypeScript environments, the `check` has been asserted true, so
 any further code does not need additional compile-time checks.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `check` | `unknown` | The value to check. |
| `message` | `string` | The error message. |
| `name` | `string` | The name of the argument. |
| `value` | `unknown` | The value of the argument. |

## Returns

`asserts check`

## Throws

Throws if `check` is falsish.

## Source

[src.ts/utils/errors.ts:771](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/utils/errors.ts#L771)
