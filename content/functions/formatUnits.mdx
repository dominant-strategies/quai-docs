---
title: formatUnits
---

```ts
function formatUnits(value, unit?): string
```

Converts `value` into a //decimal string//, assuming `unit` decimal places. The `unit` may be the number of decimal
places or the name of a unit (e.g. `"gwei"` for 9 decimal places).

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `value` | [`BigNumberish`](/content/type-aliases/BigNumberish) | The value to convert. |
| `unit`? | `string` \| [`Numeric`](/content/type-aliases/Numeric) | The unit to convert to. Default is `18` |

## Returns

`string`

The converted value.

## Throws

If the unit is invalid.

## Source

[utils/units.ts:34](https://github.com/dominant-strategies/quais.js/blob/40f3917383e9421179ebdbc4076b907a7d088144/src/utils/units.ts#L34)
