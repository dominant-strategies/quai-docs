---
title: dataSlice
---

```ts
function dataSlice(
   data, 
   start?, 
   end?): string
```

Returns a DataHexString | **DataHexString** by slicing `data` from the `start` offset to the `end` offset.

By default `start` is 0 and `end` is the length of `data`.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike) | The data to slice. |
| `start`? | `number` | The start offset. |
| `end`? | `number` | The end offset. |

## Returns

`string`

The sliced data.

## Source

[src/utils/data.ts:178](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/utils/data.ts#L178)
