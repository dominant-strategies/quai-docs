[**quais6**](../README.md) • **Docs**

***

[quais6](../globals.md) / encryptKeystoreJson

# Function: encryptKeystoreJson()

> **encryptKeystoreJson**(`account`, `password`, `options`?): `Promise`\<`string`\>

Resolved to the JSON Keystore Wallet for `account` encrypted
 with `password`.

 The `options` can be used to tune the password-based key
 derivation function parameters, explicitly set the random values
 used and provide a [**ProgressCallback**](../type-aliases/ProgressCallback.md) to receive periodic updates
 on the completion status..

## Parameters

• **account**: [`KeystoreAccount`](../type-aliases/KeystoreAccount.md)

The account to encrypt.

• **password**: `string` \| `Uint8Array`

The password to encrypt the JSON data.

• **options?**: [`EncryptOptions`](../type-aliases/EncryptOptions.md)

The options to use when encrypting the JSON data.

## Returns

`Promise`\<`string`\>

The encrypted JSON data.

## Source

[src.ts/wallet/json-keystore.ts:417](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/json-keystore.ts#L417)
