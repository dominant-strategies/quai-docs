---
title: decryptKeystoreJson
description: ""
---

```ts
function decryptKeystoreJson(
   json, 
   _password, 
progress?): Promise<KeystoreAccount>
```

Resolves to the decrypted JSON Keystore Wallet `json` using the
 `password`.

 If provided, `progress` will be called periodically during the
 decrpytion to provide feedback, and if the function returns
 `false` will halt decryption.

 The `progressCallback` will **always** receive `0` before
 decryption begins and `1` when complete.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `json` | `string` | The JSON data to decrypt. |
| `_password` | `string` \| `Uint8Array` | The password to decrypt the JSON data. |
| `progress`? | [`ProgressCallback`](/content/type-aliases/ProgressCallback.mdx) | The callback to receive progress updates. |

## Returns

`Promise`\<[`KeystoreAccount`](/content/type-aliases/KeystoreAccount.mdx)\>

The decrypted account.

## Source

[src.ts/wallet/json-keystore.ts:250](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/json-keystore.ts#L250)
