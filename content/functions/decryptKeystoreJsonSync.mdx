[**quais6**](../README.md) • **Docs**

***

[quais6](../globals.md) / decryptKeystoreJsonSync

# Function: decryptKeystoreJsonSync()

> **decryptKeystoreJsonSync**(`json`, `_password`): [`KeystoreAccount`](../type-aliases/KeystoreAccount.md)

Returns the account details for the JSON Keystore Wallet `json`
 using `password`.

 It is preferred to use the [**async version**](decryptKeystoreJson.md)
 instead, which allows a [**ProgressCallback](../type-aliases/ProgressCallback.md) to keep the user informed
 as to the decryption status.

 This method will block the event loop (freezing all UI) until decryption
 is complete, which can take quite some time, depending on the wallet
 paramters and platform.

## Parameters

• **json**: `string`

The JSON data to decrypt.

• **\_password**: `string` \| `Uint8Array`

The password to decrypt the JSON data.

## Returns

[`KeystoreAccount`](../type-aliases/KeystoreAccount.md)

The decrypted account.

## Source

[src.ts/wallet/json-keystore.ts:209](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/json-keystore.ts#L209)
