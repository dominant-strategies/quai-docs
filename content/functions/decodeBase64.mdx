---
title: decodeBase64
---

```ts
function decodeBase64(value): Uint8Array
```

Decodes the base-64 encoded `value`.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `value` | `string` | The base-64 encoded value. |

## Returns

`Uint8Array`

The decoded binary data.

## Example

```ts
// The decoded value is always binary data...
result = decodeBase64('SGVsbG8gV29ybGQhIQ==');
//_result:

// ...use toUtf8String to convert it to a string.
toUtf8String(result);
//_result:

// Decoding binary data
decodeBase64('EjQ=');
//_result:
```

## Source

[encoding/base64.ts:34](https://github.com/dominant-strategies/quais.js/blob/7ad0a969a30d30fc2cf79fa0dcda980927cb6fd5/src/encoding/base64.ts#L34)
