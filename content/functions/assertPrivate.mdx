---
title: assertPrivate
description: ""
---

```ts
function assertPrivate(
   givenGuard, 
   guard, 
   className?): void
```

Many classes use file-scoped values to guard the constructor,
 making it effectively private. This facilitates that pattern
 by ensuring the `givenGaurd` matches the file-scoped `guard`,
 throwing if not, indicating the `className%% if provided.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `givenGuard` | `any` | The guard provided to the constructor. |
| `guard` | `any` | The file-scoped guard. |
| `className`? | `string` | The class name. |

## Returns

`void`

## Throws

Throws if the guards do not match.

## Source

[src.ts/utils/errors.ts:838](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/utils/errors.ts#L838)
