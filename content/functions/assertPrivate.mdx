[**quais6**](../README.md) • **Docs**

***

[quais6](../globals.md) / assertPrivate

# Function: assertPrivate()

> **assertPrivate**(`givenGuard`, `guard`, `className`?): `void`

Many classes use file-scoped values to guard the constructor,
 making it effectively private. This facilitates that pattern
 by ensuring the `givenGaurd` matches the file-scoped `guard`,
 throwing if not, indicating the `className%% if provided.

## Parameters

• **givenGuard**: `any`

The guard provided to the constructor.

• **guard**: `any`

The file-scoped guard.

• **className?**: `string`

The class name.

## Returns

`void`

## Throws

Throws if the guards do not match.

## Source

[src.ts/utils/errors.ts:838](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/utils/errors.ts#L838)
