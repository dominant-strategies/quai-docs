---
title: assertPrivate
---

```ts
function assertPrivate(
   givenGuard, 
   guard, 
   className?): void
```

Many classes use file-scoped values to guard the constructor, making it effectively private. This facilitates that
pattern by ensuring the `givenGaurd` matches the file-scoped `guard`, throwing if not, indicating the `className%% if
provided.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `givenGuard` | `any` | The guard provided to the constructor. |
| `guard` | `any` | The file-scoped guard. |
| `className`? | `string` | The class name. |

## Returns

`void`

## Throws

Throws if the guards do not match.

## Source

[src/utils/errors.ts:841](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/utils/errors.ts#L841)
