---
title: VoidSigner
---

A **VoidSigner** is a class deisgned to allow an address to be used in any API which accepts a Signer, but for which
there are no credentials available to perform any actual signing.

This for example allow impersonating an account for the purpose of static calls or estimating gas, but does not allow
sending transactions.

## Extends

- [`AbstractSigner`](/content/classes/AbstractSigner)

## Extended by

- [`HDNodeVoidWallet`](/content/classes/HDNodeVoidWallet)

## Constructors

### new VoidSigner()

```ts
new VoidSigner(address, provider?): VoidSigner
```

Creates a new **VoidSigner** with `address` attached to `provider`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `address` | `string` |
| `provider`? | `null` \| [`Provider`](/content/interfaces/Provider) |

#### Returns

[`VoidSigner`](/content/classes/VoidSigner)

#### Overrides

[`AbstractSigner`](/content/classes/AbstractSigner).[`constructor`](/content/classes/AbstractSigner#constructors)

#### Source

[src/signers/abstract-signer.ts:224](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/signers/abstract-signer.ts#L224)

## Properties

| Property | Modifier | Type | Description | Inherited from |
| :------ | :------ | :------ | :------ | :------ |
| `address` | `readonly` | `string` | The signer address. | - |
| `provider` | `readonly` | `null` \| [`Provider`](/content/interfaces/Provider) | The provider this signer is connected to. | [`AbstractSigner`](/content/classes/AbstractSigner).`provider` |
