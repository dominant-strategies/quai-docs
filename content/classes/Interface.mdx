---
title: Interface
description: An Interface abstracts many of the low-level details for  encoding
  and decoding the data on the blockchain.   An ABI provides information on how
  to encode data to send to  a Contract, how to decode the results and events
  and how to  interpret revert errors.   The ABI can be specified by ...
---

An Interface abstracts many of the low-level details for
 encoding and decoding the data on the blockchain.

 An ABI provides information on how to encode data to send to
 a Contract, how to decode the results and events and how to
 interpret revert errors.

 The ABI can be specified by [any supported format](/content/type-aliases/InterfaceAbi.mdx).

## Constructors

### new Interface()

```ts
new Interface(fragments): Interface
```

Create a new Interface for the fragments.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `fragments` | [`InterfaceAbi`](/content/type-aliases/InterfaceAbi.mdx) | The ABI fragments. |

#### Returns

[`Interface`](/content/classes/Interface.mdx)

#### Source

[src.ts/abi/interface.ts:342](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L342)

## Properties

| Property | Modifier | Type | Description |
| :------ | :------ | :------ | :------ |
| `deploy` | `readonly` | [`ConstructorFragment`](/content/classes/ConstructorFragment.mdx) | The Contract constructor. |
| `fallback` | `readonly` | `null` \| [`FallbackFragment`](/content/classes/FallbackFragment.mdx) | The Fallback method, if any. |
| `fragments` | `readonly` | readonly [`Fragment`](/content/classes/Fragment.mdx)[] | All the Contract ABI members (i.e. methods, events, errors, etc). |
| `receive` | `readonly` | `boolean` | If receiving ether is supported. |

## Methods

### decodeErrorResult()

```ts
decodeErrorResult(fragment, data): Result
```

Decodes the result `data` (e.g. from an `quai_call`) for the
 specified error (see [**getError**](/content/classes/Interface.mdx#geterror) for valid values for
 `key`).

 Most developers should prefer the parseCallResult | **parseCallResult** method instead,
 which will automatically detect a `CALL_EXCEPTION` and throw the
 corresponding error.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`ErrorFragment`](/content/classes/ErrorFragment.mdx) |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike.mdx) |

#### Returns

[`Result`](/content/classes/Result.mdx)

#### Source

[src.ts/abi/interface.ts:830](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L830)

***

### decodeFunctionData()

```ts
decodeFunctionData(fragment, data): Result
```

Decodes the `data` from a transaction `tx.data` for
 the function specified (see [**getFunction**](/content/classes/Interface.mdx#getfunction) for valid values
 for `fragment`).

 Most developers should prefer the [**parseTransaction**](/content/classes/Interface.mdx#parsetransaction) method
 instead, which will automatically detect the fragment.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment.mdx) |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike.mdx) |

#### Returns

[`Result`](/content/classes/Result.mdx)

#### Source

[src.ts/abi/interface.ts:872](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L872)

***

### decodeFunctionResult()

```ts
decodeFunctionResult(fragment, data): Result
```

Decodes the result `data` (e.g. from an `quai_call`) for the
 specified function (see [**getFunction**](/content/classes/Interface.mdx#getfunction) for valid values for
 `key`).

 Most developers should prefer the parseCallResult | **parseCallResult** method instead,
 which will automatically detect a `CALL_EXCEPTION` and throw the
 corresponding error.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment.mdx) |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike.mdx) |

#### Returns

[`Result`](/content/classes/Result.mdx)

#### Source

[src.ts/abi/interface.ts:912](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L912)

***

### encodeDeploy()

```ts
encodeDeploy(values?): string
```

Encodes a `tx.data` object for deploying the Contract with
 the `values` as the constructor arguments.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src.ts/abi/interface.ts:817](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L817)

***

### encodeErrorResult()

```ts
encodeErrorResult(fragment, values?): string
```

Encodes the transaction revert data for a call result that
 reverted from the the Contract with the sepcified `error`
 (see [**getError**](/content/classes/Interface.mdx#geterror) for valid values for `fragment`) with the `values`.

 This is generally not used by most developers, unless trying to mock
 a result from a Contract.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`ErrorFragment`](/content/classes/ErrorFragment.mdx) |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src.ts/abi/interface.ts:851](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L851)

***

### encodeFunctionData()

```ts
encodeFunctionData(fragment, values?): string
```

Encodes the `tx.data` for a transaction that calls the function
 specified (see [**getFunction**](/content/classes/Interface.mdx#getfunction) for valid values for `fragment`) with
 the `values`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment.mdx) |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src.ts/abi/interface.ts:890](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L890)

***

### encodeFunctionResult()

```ts
encodeFunctionResult(fragment, values?): string
```

Encodes the result data (e.g. from an `quai_call`) for the
 specified function (see [**getFunction**](/content/classes/Interface.mdx#getfunction) for valid values
 for `fragment`) with `values`.

 This is generally not used by most developers, unless trying to mock
 a result from a Contract.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment.mdx) |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src.ts/abi/interface.ts:983](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L983)

***

### forEachError()

```ts
forEachError(callback): void
```

Iterate over all errors, calling `callback`, sorted by their name.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `callback` | (`func`, `index`) => `void` |

#### Returns

`void`

#### Source

[src.ts/abi/interface.ts:765](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L765)

***

### forEachEvent()

```ts
forEachEvent(callback): void
```

Iterate over all events, calling `callback`, sorted by their name.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `callback` | (`func`, `index`) => `void` |

#### Returns

`void`

#### Source

[src.ts/abi/interface.ts:698](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L698)

***

### forEachFunction()

```ts
forEachFunction(callback): void
```

Iterate over all functions, calling `callback`, sorted by their name.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `callback` | (`func`, `index`) => `void` |

#### Returns

`void`

#### Source

[src.ts/abi/interface.ts:589](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L589)

***

### format()

```ts
format(minimal?): string[]
```

Returns the entire Human-Readable ABI, as an array of
 signatures, optionally as `minimal` strings, which
 removes parameter names and unneceesary spaces.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `minimal`? | `boolean` |

#### Returns

`string`[]

#### Source

[src.ts/abi/interface.ts:439](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L439)

***

### formatJson()

```ts
formatJson(): string
```

Return the JSON-encoded ABI. This is the format Solidiy
 returns.

#### Returns

`string`

#### Source

[src.ts/abi/interface.ts:449](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L449)

***

### getAbiCoder()

```ts
getAbiCoder(): AbiCoder
```

The ABI coder that will be used to encode and decode binary
 data.

#### Returns

[`AbiCoder`](/content/classes/AbiCoder.mdx)

#### Source

[src.ts/abi/interface.ts:460](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L460)

***

### getError()

```ts
getError(key, values?): null | ErrorFragment
```

Get the [**ErroFragment**](/content/classes/ErrorFragment.mdx) for `key`, which may be an error
 selector, error name or error signature that belongs to the ABI.

 If `values` is provided, it will use the Typed API to handle
 ambiguous cases where multiple errors match by name.

 If the `key` and `values` do not refine to a single error in
 the ABI, this will throw.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |
| `values`? | `any`[] |

#### Returns

`null` \| [`ErrorFragment`](/content/classes/ErrorFragment.mdx)

#### Source

[src.ts/abi/interface.ts:717](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L717)

***

### getEvent()

```ts
getEvent(key, values?): null | EventFragment
```

Get the [**EventFragment**](/content/classes/EventFragment.mdx) for `key`, which may be a topic hash,
 event name or event signature that belongs to the ABI.

 If `values` is provided, it will use the Typed API to handle
 ambiguous cases where multiple events match by name.

 If the `key` and `values` do not refine to a single event in
 the ABI, this will throw.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |
| `values`? | `any`[] |

#### Returns

`null` \| [`EventFragment`](/content/classes/EventFragment.mdx)

#### Source

[src.ts/abi/interface.ts:691](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L691)

***

### getEventName()

```ts
getEventName(key): string
```

Get the event name for `key`, which may be a topic hash,
 event name or event signature that belongs to the ABI.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`string`

#### Source

[src.ts/abi/interface.ts:663](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L663)

***

### getFunction()

```ts
getFunction(key, values?): null | FunctionFragment
```

Get the [**FunctionFragment**](/content/classes/FunctionFragment.mdx) for `key`, which may be a function
 selector, function name or function signature that belongs to the ABI.

 If `values` is provided, it will use the Typed API to handle
 ambiguous cases where multiple functions match by name.

 If the `key` and `values` do not refine to a single function in
 the ABI, this will throw.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |
| `values`? | `any`[] |

#### Returns

`null` \| [`FunctionFragment`](/content/classes/FunctionFragment.mdx)

#### Source

[src.ts/abi/interface.ts:582](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L582)

***

### getFunctionName()

```ts
getFunctionName(key): string
```

Get the function name for `key`, which may be a function selector,
 function name or function signature that belongs to the ABI.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`string`

#### Source

[src.ts/abi/interface.ts:555](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L555)

***

### hasEvent()

```ts
hasEvent(key): boolean
```

Returns true if `key` (an event topic hash, event name or
 event signature) is present in the ABI.

 In the case of an event name, the name may be ambiguous, so
 accessing the [**EventFragment**](/content/classes/EventFragment.mdx) may require refinement.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`boolean`

#### Source

[src.ts/abi/interface.ts:677](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L677)

***

### hasFunction()

```ts
hasFunction(key): boolean
```

Returns true if `key` (a function selector, function name or
 function signature) is present in the ABI.

 In the case of a function name, the name may be ambiguous, so
 accessing the [**FunctionFragment**](/content/classes/FunctionFragment.mdx) may require refinement.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`boolean`

#### Source

[src.ts/abi/interface.ts:568](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L568)

***

### parseError()

```ts
parseError(data): null | ErrorDescription
```

Parses a revert data, finding the matching error and extracts
 the parameter values along with other useful error details.

 If the matching error cannot be found, returns null.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike.mdx) |

#### Returns

`null` \| [`ErrorDescription`](/content/classes/ErrorDescription.mdx)

#### Source

[src.ts/abi/interface.ts:1249](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L1249)

***

### parseLog()

```ts
parseLog(log): null | LogDescription
```

Parses a receipt log, finding the matching event and extracts
 the parameter values along with other useful event details.

 If the matching event cannot be found, returns null.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `log` | `object` |
| `log.data` | `string` |
| `log.topics` | `string`[] |

#### Returns

`null` \| [`LogDescription`](/content/classes/LogDescription.mdx)

#### Source

[src.ts/abi/interface.ts:1230](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L1230)

***

### parseTransaction()

```ts
parseTransaction(tx): null | TransactionDescription
```

Parses a transaction, finding the matching function and extracts
 the parameter values along with other useful function details.

 If the matching function cannot be found, return null.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `tx` | `object` |
| `tx.data` | `string` |
| `tx.value`? | [`BigNumberish`](/content/type-aliases/BigNumberish.mdx) |

#### Returns

`null` \| [`TransactionDescription`](/content/classes/TransactionDescription.mdx)

#### Source

[src.ts/abi/interface.ts:1208](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L1208)

***

### from()

```ts
static from(value): Interface
```

Creates a new [**Interface**](/content/classes/Interface.mdx) from the ABI `value`.

 The `value` may be provided as an existing [**Interface**](/content/classes/Interface.mdx) object,
 a JSON-encoded ABI or any Human-Readable ABI format.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `value` | [`Interface`](/content/classes/Interface.mdx) \| [`InterfaceAbi`](/content/type-aliases/InterfaceAbi.mdx) |

#### Returns

[`Interface`](/content/classes/Interface.mdx)

#### Source

[src.ts/abi/interface.ts:1266](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/abi/interface.ts#L1266)
