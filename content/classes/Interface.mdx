---
title: Interface
---

An Interface abstracts many of the low-level details for encoding and decoding the data on the blockchain.

An ABI provides information on how to encode data to send to a Contract, how to decode the results and events and how
to interpret revert errors.

The ABI can be specified by [any supported format](/content/type-aliases/InterfaceAbi).

## Constructors

### new Interface()

```ts
new Interface(fragments): Interface
```

Create a new Interface for the fragments.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `fragments` | [`InterfaceAbi`](/content/type-aliases/InterfaceAbi) | The ABI fragments. |

#### Returns

[`Interface`](/content/classes/Interface)

#### Source

[src/abi/interface.ts:344](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L344)

## Properties

| Property | Modifier | Type | Description |
| :------ | :------ | :------ | :------ |
| `deploy` | `readonly` | [`ConstructorFragment`](/content/classes/ConstructorFragment) | The Contract constructor. |
| `fallback` | `readonly` | `null` \| [`FallbackFragment`](/content/classes/FallbackFragment) | The Fallback method, if any. |
| `fragments` | `readonly` | readonly [`Fragment`](/content/classes/Fragment)[] | All the Contract ABI members (i.e. methods, events, errors, etc). |
| `receive` | `readonly` | `boolean` | If receiving ether is supported. |

## Methods

### decodeErrorResult()

```ts
decodeErrorResult(fragment, data): Result
```

Decodes the result `data` (e.g. from an `quai_call`) for the specified error (see [**getError**](/content/classes/Interface#geterror)
for valid values for `key`).

Most developers should prefer the parseCallResult | **parseCallResult** method instead, which will
automatically detect a `CALL_EXCEPTION` and throw the corresponding error.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`ErrorFragment`](/content/classes/ErrorFragment) |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike) |

#### Returns

[`Result`](/content/classes/Result)

#### Source

[src/abi/interface.ts:822](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L822)

***

### decodeFunctionData()

```ts
decodeFunctionData(fragment, data): Result
```

Decodes the `data` from a transaction `tx.data` for the function specified (see
[**getFunction**](/content/classes/Interface#getfunction) for valid values for `fragment`).

Most developers should prefer the [**parseTransaction**](/content/classes/Interface#parsetransaction) method instead, which will
automatically detect the fragment.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment) |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike) |

#### Returns

[`Result`](/content/classes/Result)

#### Source

[src/abi/interface.ts:862](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L862)

***

### decodeFunctionResult()

```ts
decodeFunctionResult(fragment, data): Result
```

Decodes the result `data` (e.g. from an `quai_call`) for the specified function (see
[**getFunction**](/content/classes/Interface#getfunction) for valid values for `key`).

Most developers should prefer the parseCallResult | **parseCallResult** method instead, which will
automatically detect a `CALL_EXCEPTION` and throw the corresponding error.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment) |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike) |

#### Returns

[`Result`](/content/classes/Result)

#### Source

[src/abi/interface.ts:900](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L900)

***

### encodeDeploy()

```ts
encodeDeploy(values?): string
```

Encodes a `tx.data` object for deploying the Contract with the `values` as the constructor arguments.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src/abi/interface.ts:811](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L811)

***

### encodeErrorResult()

```ts
encodeErrorResult(fragment, values?): string
```

Encodes the transaction revert data for a call result that reverted from the the Contract with the sepcified
`error` (see [**getError**](/content/classes/Interface#geterror) for valid values for `fragment`) with the `values`.

This is generally not used by most developers, unless trying to mock a result from a Contract.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`ErrorFragment`](/content/classes/ErrorFragment) |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src/abi/interface.ts:845](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L845)

***

### encodeFunctionData()

```ts
encodeFunctionData(fragment, values?): string
```

Encodes the `tx.data` for a transaction that calls the function specified (see
[**getFunction**](/content/classes/Interface#getfunction) for valid values for `fragment`) with the `values`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment) |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src/abi/interface.ts:883](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L883)

***

### encodeFunctionResult()

```ts
encodeFunctionResult(fragment, values?): string
```

Encodes the result data (e.g. from an `quai_call`) for the specified function (see
[**getFunction**](/content/classes/Interface#getfunction) for valid values for `fragment`) with `values`.

This is generally not used by most developers, unless trying to mock a result from a Contract.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `fragment` | `string` \| [`FunctionFragment`](/content/classes/FunctionFragment) |
| `values`? | readonly `any`[] |

#### Returns

`string`

#### Source

[src/abi/interface.ts:971](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L971)

***

### forEachError()

```ts
forEachError(callback): void
```

Iterate over all errors, calling `callback`, sorted by their name.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `callback` | (`func`, `index`) => `void` |

#### Returns

`void`

#### Source

[src/abi/interface.ts:791](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L791)

***

### forEachEvent()

```ts
forEachEvent(callback): void
```

Iterate over all events, calling `callback`, sorted by their name.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `callback` | (`func`, `index`) => `void` |

#### Returns

`void`

#### Source

[src/abi/interface.ts:710](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L710)

***

### forEachFunction()

```ts
forEachFunction(callback): void
```

Iterate over all functions, calling `callback`, sorted by their name.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `callback` | (`func`, `index`) => `void` |

#### Returns

`void`

#### Source

[src/abi/interface.ts:597](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L597)

***

### format()

```ts
format(minimal?): string[]
```

Returns the entire Human-Readable ABI, as an array of signatures, optionally as `minimal` strings, which removes
parameter names and unneceesary spaces.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `minimal`? | `boolean` |

#### Returns

`string`[]

#### Source

[src/abi/interface.ts:440](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L440)

***

### formatJson()

```ts
formatJson(): string
```

Return the JSON-encoded ABI. This is the format Solidiy returns.

#### Returns

`string`

#### Source

[src/abi/interface.ts:449](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L449)

***

### getAbiCoder()

```ts
getAbiCoder(): AbiCoder
```

The ABI coder that will be used to encode and decode binary data.

#### Returns

[`AbiCoder`](/content/classes/AbiCoder)

#### Source

[src/abi/interface.ts:459](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L459)

***

### getError()

```ts
getError(key, values?): null | ErrorFragment
```

Get the [**ErroFragment**](/content/classes/ErrorFragment) for `key`, which may be an error selector, error name or error
signature that belongs to the ABI.

If `values` is provided, it will use the Typed API to handle ambiguous cases where multiple errors match by name.

If the `key` and `values` do not refine to a single error in the ABI, this will throw.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |
| `values`? | `any`[] |

#### Returns

`null` \| [`ErrorFragment`](/content/classes/ErrorFragment)

#### Source

[src/abi/interface.ts:729](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L729)

***

### getEvent()

```ts
getEvent(key, values?): null | EventFragment
```

Get the [**EventFragment**](/content/classes/EventFragment) for `key`, which may be a topic hash, event name or event
signature that belongs to the ABI.

If `values` is provided, it will use the Typed API to handle ambiguous cases where multiple events match by name.

If the `key` and `values` do not refine to a single event in the ABI, this will throw.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |
| `values`? | `any`[] |

#### Returns

`null` \| [`EventFragment`](/content/classes/EventFragment)

#### Source

[src/abi/interface.ts:703](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L703)

***

### getEventName()

```ts
getEventName(key): string
```

Get the event name for `key`, which may be a topic hash, event name or event signature that belongs to the ABI.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`string`

#### Source

[src/abi/interface.ts:678](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L678)

***

### getFunction()

```ts
getFunction(key, values?): null | FunctionFragment
```

Get the [**FunctionFragment**](/content/classes/FunctionFragment) for `key`, which may be a function selector, function name
or function signature that belongs to the ABI.

If `values` is provided, it will use the Typed API to handle ambiguous cases where multiple functions match by
name.

If the `key` and `values` do not refine to a single function in the ABI, this will throw.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |
| `values`? | `any`[] |

#### Returns

`null` \| [`FunctionFragment`](/content/classes/FunctionFragment)

#### Source

[src/abi/interface.ts:590](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L590)

***

### getFunctionName()

```ts
getFunctionName(key): string
```

Get the function name for `key`, which may be a function selector, function name or function signature that
belongs to the ABI.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`string`

#### Source

[src/abi/interface.ts:565](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L565)

***

### hasEvent()

```ts
hasEvent(key): boolean
```

Returns true if `key` (an event topic hash, event name or event signature) is present in the ABI.

In the case of an event name, the name may be ambiguous, so accessing the
[**EventFragment**](/content/classes/EventFragment) may require refinement.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`boolean`

#### Source

[src/abi/interface.ts:691](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L691)

***

### hasFunction()

```ts
hasFunction(key): boolean
```

Returns true if `key` (a function selector, function name or function signature) is present in the ABI.

In the case of a function name, the name may be ambiguous, so accessing the
[**FunctionFragment**](/content/classes/FunctionFragment) may require refinement.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` |

#### Returns

`boolean`

#### Source

[src/abi/interface.ts:577](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L577)

***

### parseError()

```ts
parseError(data): null | ErrorDescription
```

Parses a revert data, finding the matching error and extracts the parameter values along with other useful error
details.

If the matching error cannot be found, returns null.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `data` | [`BytesLike`](/content/type-aliases/BytesLike) |

#### Returns

`null` \| [`ErrorDescription`](/content/classes/ErrorDescription)

#### Source

[src/abi/interface.ts:1237](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L1237)

***

### parseLog()

```ts
parseLog(log): null | LogDescription
```

Parses a receipt log, finding the matching event and extracts the parameter values along with other useful event
details.

If the matching event cannot be found, returns null.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `log` | `object` |
| `log.data` | `string` |
| `log.topics` | `string`[] |

#### Returns

`null` \| [`LogDescription`](/content/classes/LogDescription)

#### Source

[src/abi/interface.ts:1217](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L1217)

***

### parseTransaction()

```ts
parseTransaction(tx): null | TransactionDescription
```

Parses a transaction, finding the matching function and extracts the parameter values along with other useful
function details.

If the matching function cannot be found, return null.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `tx` | `object` |
| `tx.data` | `string` |
| `tx.value`? | [`BigNumberish`](/content/type-aliases/BigNumberish) |

#### Returns

`null` \| [`TransactionDescription`](/content/classes/TransactionDescription)

#### Source

[src/abi/interface.ts:1191](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L1191)

***

### from()

```ts
static from(value): Interface
```

Creates a new [**Interface**](/content/classes/Interface) from the ABI `value`.

The `value` may be provided as an existing [**Interface**](/content/classes/Interface) object, a JSON-encoded ABI or any
Human-Readable ABI format.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `value` | [`Interface`](/content/classes/Interface) \| [`InterfaceAbi`](/content/type-aliases/InterfaceAbi) |

#### Returns

[`Interface`](/content/classes/Interface)

#### Source

[src/abi/interface.ts:1256](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/abi/interface.ts#L1256)
