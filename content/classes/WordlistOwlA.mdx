---
title: WordlistOwlA
description: An OWL-A format Wordlist extends the OWL format to add an  overlay
  onto an OWL format Wordlist to support diacritic  marks.   This class is
  generally not useful to most developers as  it is used mainly internally to
  keep Wordlists for languages  based on latin-1 small.   If necessary, there
  are tools within the ...
---

An OWL-A format Wordlist extends the OWL format to add an
 overlay onto an OWL format Wordlist to support diacritic
 marks.

 This class is generally not useful to most developers as
 it is used mainly internally to keep Wordlists for languages
 based on latin-1 small.

 If necessary, there are tools within the `generation/` folder
 to create the necessary data.

## Extends

- [`WordlistOwl`](/content/classes/WordlistOwl.mdx)

## Extended by

- [`LangEs`](/content/classes/LangEs.mdx)

## Constructors

### new WordlistOwlA()

```ts
new WordlistOwlA(
   locale, 
   data, 
   accent, 
   checksum): WordlistOwlA
```

Creates a new Wordlist for `locale` using the OWLA `data`
 and `accent` data and validated against the `checksum`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `locale` | `string` |
| `data` | `string` |
| `accent` | `string` |
| `checksum` | `string` |

#### Returns

[`WordlistOwlA`](/content/classes/WordlistOwlA.mdx)

#### Overrides

[`WordlistOwl`](/content/classes/WordlistOwl.mdx).[`constructor`](/content/classes/WordlistOwl.mdx#constructors)

#### Source

[src.ts/wordlists/wordlist-owla.ts:27](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wordlists/wordlist-owla.ts#L27)

## Accessors

### \_accent

```ts
get _accent(): string
```

The OWLA-encoded accent data.

#### Returns

`string`

#### Source

[src.ts/wordlists/wordlist-owla.ts:35](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wordlists/wordlist-owla.ts#L35)

***

### \_data

```ts
get _data(): string
```

The OWL-encoded data.

#### Returns

`string`

#### Source

[src.ts/wordlists/wordlist-owl.ts:43](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wordlists/wordlist-owl.ts#L43)

## Methods

### \_decodeWords()

```ts
_decodeWords(): string[]
```

Decode all the words for the wordlist.

#### Returns

`string`[]

#### Overrides

[`WordlistOwl`](/content/classes/WordlistOwl.mdx).[`_decodeWords`](/content/classes/WordlistOwl.mdx#_decodewords)

#### Source

[src.ts/wordlists/wordlist-owla.ts:40](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wordlists/wordlist-owla.ts#L40)

***

### join()

```ts
join(words): string
```

Sub-classes may override this to provider a language-specific
 method for joining `words` into a phrase.

 By default, `words` are joined by a single space.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `words` | `string`[] | The words to join. |

#### Returns

`string`

The joined phrase.

#### Inherited from

[`WordlistOwl`](/content/classes/WordlistOwl.mdx).[`join`](/content/classes/WordlistOwl.mdx#join)

#### Source

[src.ts/wordlists/wordlist.ts:50](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wordlists/wordlist.ts#L50)

***

### split()

```ts
split(phrase): string[]
```

Sub-classes may override this to provide a language-specific
 method for spliting `phrase` into individual words.

 By default, `phrase` is split using any sequences of
 white-space as defined by regular expressions (i.e. `/\s+/`).

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `phrase` | `string` | The phrase to split. |

#### Returns

`string`[]

The split words in the phrase.

#### Inherited from

[`WordlistOwl`](/content/classes/WordlistOwl.mdx).[`split`](/content/classes/WordlistOwl.mdx#split)

#### Source

[src.ts/wordlists/wordlist.ts:37](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wordlists/wordlist.ts#L37)
