---
title: Wallet
---

A **Wallet** manages a single private key which is used to sign transactions, messages and other common payloads.

This class is generally the main entry point for developers that wish to use a private key directly, as it can create
instances from a large variety of common sources, including raw private key,
[BIP-39](https://en.bitcoin.it/wiki/BIP_0039) mnemonics and encrypte JSON wallets.

## Extends

- [`BaseWallet`](/content/classes/BaseWallet)

## Constructors

### new Wallet()

```ts
new Wallet(key, provider?): Wallet
```

Create a new wallet for the private `key`, optionally connected to `provider`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` \| [`SigningKey`](/content/classes/SigningKey) |
| `provider`? | `null` \| [`Provider`](/content/interfaces/Provider) |

#### Returns

[`Wallet`](/content/classes/Wallet)

#### Overrides

[`BaseWallet`](/content/classes/BaseWallet).[`constructor`](/content/classes/BaseWallet#constructors)

#### Source

[src/wallet/wallet.ts:29](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/wallet.ts#L29)

## Properties

| Property | Modifier | Type | Description | Inherited from |
| :------ | :------ | :------ | :------ | :------ |
| `#address` | `private` | `string` | The wallet address. | [`BaseWallet`](/content/classes/BaseWallet).`#address` |
| `provider` | `readonly` | `null` \| [`Provider`](/content/interfaces/Provider) | The provider this signer is connected to. | [`BaseWallet`](/content/classes/BaseWallet).`provider` |

## Accessors

### address

```ts
get address(): string
```

The address of this wallet.

#### Returns

`string`

#### Source

[src/wallet/base-wallet.ts:57](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/base-wallet.ts#L57)

***

### privateKey

```ts
get privateKey(): string
```

The private key for this wallet.

#### Returns

`string`

#### Source

[src/wallet/base-wallet.ts:71](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/base-wallet.ts#L71)

***

### signingKey

```ts
get signingKey(): SigningKey
```

The [**SigningKey**](/content/classes/SigningKey) used for signing payloads.

#### Returns

[`SigningKey`](/content/classes/SigningKey)

#### Source

[src/wallet/base-wallet.ts:64](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/base-wallet.ts#L64)

## Methods

### encrypt()

```ts
encrypt(password, progressCallback?): Promise<string>
```

Resolves to a [JSON Keystore Wallet](json-wallets) encrypted with `password`.

If `progressCallback` is specified, it will receive periodic updates as the encryption process progreses.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `password` | `string` \| `Uint8Array` | The password to encrypt the wallet with. |
| `progressCallback`? | [`ProgressCallback`](/content/type-aliases/ProgressCallback) | An optional callback to keep the user informed. |

#### Returns

`Promise`\<`string`\>

The encrypted JSON wallet.

#### Source

[src/wallet/wallet.ts:52](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/wallet.ts#L52)

***

### encryptSync()

```ts
encryptSync(password): string
```

Returns a [JSON Keystore Wallet](json-wallets) encryped with `password`.

It is preferred to use the [async version](encrypt) instead, which allows a
[**ProgressCallback**](/content/type-aliases/ProgressCallback) to keep the user informed.

This method will block the event loop (freezing all UI) until it is complete, which may be a non-trivial
duration.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `password` | `string` \| `Uint8Array` | The password to encrypt the wallet with. |

#### Returns

`string`

The encrypted JSON wallet.

#### Source

[src/wallet/wallet.ts:70](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/wallet.ts#L70)

***

### signMessageSync()

```ts
signMessageSync(message): string
```

Returns the signature for `message` signed with this wallet.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `message` | `string` \| `Uint8Array` | The message to sign. |

#### Returns

`string`

The serialized signature.

#### Inherited from

[`BaseWallet`](/content/classes/BaseWallet).[`signMessageSync`](/content/classes/BaseWallet#signmessagesync)

#### Source

[src/wallet/base-wallet.ts:125](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/base-wallet.ts#L125)

***

### fromEncryptedJson()

```ts
static fromEncryptedJson(
   json, 
   password, 
progress?): Promise<Wallet>
```

Creates (asynchronously) a **Wallet** by decrypting the `json` with `password`.

If `progress` is provided, it is called periodically during decryption so that any UI can be updated.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `json` | `string` | The JSON data to decrypt. |
| `password` | `string` \| `Uint8Array` | The password to decrypt the JSON data. |
| `progress`? | [`ProgressCallback`](/content/type-aliases/ProgressCallback) | An optional callback to keep the user informed. |

#### Returns

`Promise`\<[`Wallet`](/content/classes/Wallet)\>

The decrypted wallet.

#### Source

[src/wallet/wallet.ts:96](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/wallet.ts#L96)

***

### fromEncryptedJsonSync()

```ts
static fromEncryptedJsonSync(json, password): Wallet | QuaiHDWallet
```

Creates a **Wallet** by decrypting the `json` with `password`.

The [**fromEncryptedJson**](/content/classes/Wallet#fromencryptedjson) method is preferred, as this method will lock up and
freeze the UI during decryption, which may take some time.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `json` | `string` | The JSON data to decrypt. |
| `password` | `string` \| `Uint8Array` | The password to decrypt the JSON data. |

#### Returns

[`Wallet`](/content/classes/Wallet) \| [`QuaiHDWallet`](/content/classes/QuaiHDWallet)

The decrypted wallet.

#### Source

[src/wallet/wallet.ts:117](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/wallet/wallet.ts#L117)
