[**quais6**](../README.md) • **Docs**

***

[quais6](../globals.md) / Wallet

# Class: Wallet

A **Wallet** manages a single private key which is used to sign
 transactions, messages and other common payloads.

 This class is generally the main entry point for developers
 that wish to use a private key directly, as it can create
 instances from a large variety of common sources, including
 raw private key, [BIP-39](https://en.bitcoin.it/wiki/BIP_0039) mnemonics and encrypte JSON
 wallets.

## Extends

- [`BaseWallet`](BaseWallet.md)

## Constructors

### new Wallet()

> **new Wallet**(`key`, `provider`?): [`Wallet`](Wallet.md)

Create a new wallet for the private `key`, optionally connected
 to `provider`.

#### Parameters

• **key**: `string` \| [`SigningKey`](SigningKey.md)

• **provider?**: `null` \| [`Provider`](../interfaces/Provider.md)

#### Returns

[`Wallet`](Wallet.md)

#### Overrides

[`BaseWallet`](BaseWallet.md).[`constructor`](BaseWallet.md#constructors)

#### Source

[src.ts/wallet/wallet.ts:44](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/wallet.ts#L44)

## Properties

### #address

> `private` `readonly` **#address**: `string`

The wallet address.

#### Inherited from

[`BaseWallet`](BaseWallet.md).[`#address`](BaseWallet.md##address)

#### Source

[src.ts/wallet/base-wallet.ts:32](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/base-wallet.ts#L32)

***

### provider

> `readonly` **provider**: `null` \| [`Provider`](../interfaces/Provider.md)

The provider this signer is connected to.

#### Inherited from

[`BaseWallet`](BaseWallet.md).[`provider`](BaseWallet.md#provider)

#### Source

[src.ts/providers/abstract-signer.ts:65](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/providers/abstract-signer.ts#L65)

## Accessors

### address

> `get` **address**(): `string`

The address of this wallet.

#### Returns

`string`

#### Source

[src.ts/wallet/base-wallet.ts:58](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/base-wallet.ts#L58)

***

### privateKey

> `get` **privateKey**(): `string`

The private key for this wallet.

#### Returns

`string`

#### Source

[src.ts/wallet/base-wallet.ts:69](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/base-wallet.ts#L69)

***

### signingKey

> `get` **signingKey**(): [`SigningKey`](SigningKey.md)

The [**SigningKey**](SigningKey.md) used for signing payloads.

#### Returns

[`SigningKey`](SigningKey.md)

#### Source

[src.ts/wallet/base-wallet.ts:64](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/base-wallet.ts#L64)

## Methods

### encrypt()

> **encrypt**(`password`, `progressCallback`?): `Promise`\<`string`\>

Resolves to a [JSON Keystore Wallet](json-wallets) encrypted with
 `password`.

 If `progressCallback` is specified, it will receive periodic
 updates as the encryption process progreses.

#### Parameters

• **password**: `string` \| `Uint8Array`

The password to encrypt the wallet with.

• **progressCallback?**: [`ProgressCallback`](../type-aliases/ProgressCallback.md)

An optional callback to keep the user informed.

#### Returns

`Promise`\<`string`\>

The encrypted JSON wallet.

#### Source

[src.ts/wallet/wallet.ts:68](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/wallet.ts#L68)

***

### encryptSync()

> **encryptSync**(`password`): `string`

Returns a [JSON Keystore Wallet](json-wallets) encryped with
 `password`.

 It is preferred to use the [async version](encrypt) instead,
 which allows a [**ProgressCallback**](../type-aliases/ProgressCallback.md) to keep the user informed.

 This method will block the event loop (freezing all UI) until
 it is complete, which may be a non-trivial duration.

#### Parameters

• **password**: `string` \| `Uint8Array`

The password to encrypt the wallet with.

#### Returns

`string`

The encrypted JSON wallet.

#### Source

[src.ts/wallet/wallet.ts:86](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/wallet.ts#L86)

***

### signMessageSync()

> **signMessageSync**(`message`): `string`

Returns the signature for `message` signed with this wallet.

#### Parameters

• **message**: `string` \| `Uint8Array`

The message to sign.

#### Returns

`string`

The serialized signature.

#### Inherited from

[`BaseWallet`](BaseWallet.md).[`signMessageSync`](BaseWallet.md#signmessagesync)

#### Source

[src.ts/wallet/base-wallet.ts:113](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/base-wallet.ts#L113)

***

### createRandom()

> `static` **createRandom**(`path`, `provider`?): [`HDNodeWallet`](HDNodeWallet.md)

Creates a new random [**HDNodeWallet**](HDNodeWallet.md) using the available
 [cryptographic random source](randomBytes).

 If there is no crytographic random source, this will throw.

#### Parameters

• **path**: `string`

The derivation path for the wallet.

• **provider?**: `null` \| [`Provider`](../interfaces/Provider.md)

An optional provider to connect the wallet to.

#### Returns

[`HDNodeWallet`](HDNodeWallet.md)

The new wallet.

#### Source

[src.ts/wallet/wallet.ts:172](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/wallet.ts#L172)

***

### fromEncryptedJson()

> `static` **fromEncryptedJson**(`json`, `password`, `progress`?): `Promise`\<[`Wallet`](Wallet.md) \| [`HDNodeWallet`](HDNodeWallet.md)\>

Creates (asynchronously) a **Wallet** by decrypting the `json`
 with `password`.

 If `progress` is provided, it is called periodically during
 decryption so that any UI can be updated.

#### Parameters

• **json**: `string`

The JSON data to decrypt.

• **password**: `string` \| `Uint8Array`

The password to decrypt the JSON data.

• **progress?**: [`ProgressCallback`](../type-aliases/ProgressCallback.md)

An optional callback to keep the user informed.

#### Returns

`Promise`\<[`Wallet`](Wallet.md) \| [`HDNodeWallet`](HDNodeWallet.md)\>

The decrypted wallet.

#### Source

[src.ts/wallet/wallet.ts:123](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/wallet.ts#L123)

***

### fromEncryptedJsonSync()

> `static` **fromEncryptedJsonSync**(`json`, `password`): [`Wallet`](Wallet.md) \| [`HDNodeWallet`](HDNodeWallet.md)

Creates a **Wallet** by decrypting the `json` with `password`.

 The [**fromEncryptedJson**](Wallet.md#fromencryptedjson) method is preferred, as this method
 will lock up and freeze the UI during decryption, which may take
 some time.

#### Parameters

• **json**: `string`

The JSON data to decrypt.

• **password**: `string` \| `Uint8Array`

The password to decrypt the JSON data.

#### Returns

[`Wallet`](Wallet.md) \| [`HDNodeWallet`](HDNodeWallet.md)

The decrypted wallet.

#### Source

[src.ts/wallet/wallet.ts:149](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/wallet.ts#L149)

***

### fromPhrase()

> `static` **fromPhrase**(`phrase`, `path`, `provider`?, `wordlist`?): [`HDNodeWallet`](HDNodeWallet.md) \| [`UTXOHDWallet`](UTXOHDWallet.md)

"m/44'/60'/0'/0/0"
 Creates a [**HDNodeWallet**](HDNodeWallet.md) for `phrase`.

#### Parameters

• **phrase**: `string`

The mnemonic phrase to create the wallet with.

• **path**: `string`

The derivation path for the wallet.

• **provider?**: [`Provider`](../interfaces/Provider.md)

An optional provider to connect the wallet to.

• **wordlist?**: [`Wordlist`](Wordlist.md)

An optional wordlist to use for the mnemonic phrase.

#### Returns

[`HDNodeWallet`](HDNodeWallet.md) \| [`UTXOHDWallet`](UTXOHDWallet.md)

The new wallet.

#### Source

[src.ts/wallet/wallet.ts:187](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/wallet/wallet.ts#L187)
