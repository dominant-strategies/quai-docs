---
title: Wallet
description: A **Wallet** manages a single private key which is used to
  sign  transactions, messages and other common payloads.   This class is
  generally the main entry point for developers  that wish to use a private key
  directly, as it can create  instances from a large variety of common sources,
  including  raw private key, [BIP-39](https://en.bitcoin.it/wiki/BIP_0039)
  mnemonics and encrypte JSON  wallets....
---

A **Wallet** manages a single private key which is used to sign
 transactions, messages and other common payloads.

 This class is generally the main entry point for developers
 that wish to use a private key directly, as it can create
 instances from a large variety of common sources, including
 raw private key, [BIP-39](https://en.bitcoin.it/wiki/BIP_0039) mnemonics and encrypte JSON
 wallets.

## Extends

- [`BaseWallet`](/content/classes/BaseWallet.mdx)

## Constructors

### new Wallet()

```ts
new Wallet(key, provider?): Wallet
```

Create a new wallet for the private `key`, optionally connected
 to `provider`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `key` | `string` \| [`SigningKey`](/content/classes/SigningKey.mdx) |
| `provider`? | `null` \| [`Provider`](/content/interfaces/Provider.mdx) |

#### Returns

[`Wallet`](/content/classes/Wallet.mdx)

#### Overrides

[`BaseWallet`](/content/classes/BaseWallet.mdx).[`constructor`](/content/classes/BaseWallet.mdx#constructors)

#### Source

[src.ts/wallet/wallet.ts:44](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/wallet.ts#L44)

## Properties

| Property | Modifier | Type | Description | Inherited from |
| :------ | :------ | :------ | :------ | :------ |
| `#address` | `private` | `string` | The wallet address. | [`BaseWallet`](/content/classes/BaseWallet.mdx).`#address` |
| `provider` | `readonly` | `null` \| [`Provider`](/content/interfaces/Provider.mdx) | The provider this signer is connected to. | [`BaseWallet`](/content/classes/BaseWallet.mdx).`provider` |

## Accessors

### address

```ts
get address(): string
```

The address of this wallet.

#### Returns

`string`

#### Source

[src.ts/wallet/base-wallet.ts:59](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/base-wallet.ts#L59)

***

### privateKey

```ts
get privateKey(): string
```

The private key for this wallet.

#### Returns

`string`

#### Source

[src.ts/wallet/base-wallet.ts:70](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/base-wallet.ts#L70)

***

### signingKey

```ts
get signingKey(): SigningKey
```

The [**SigningKey**](/content/classes/SigningKey.mdx) used for signing payloads.

#### Returns

[`SigningKey`](/content/classes/SigningKey.mdx)

#### Source

[src.ts/wallet/base-wallet.ts:65](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/base-wallet.ts#L65)

## Methods

### encrypt()

```ts
encrypt(password, progressCallback?): Promise<string>
```

Resolves to a [JSON Keystore Wallet](json-wallets) encrypted with
 `password`.

 If `progressCallback` is specified, it will receive periodic
 updates as the encryption process progreses.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `password` | `string` \| `Uint8Array` | The password to encrypt the wallet with. |
| `progressCallback`? | [`ProgressCallback`](/content/type-aliases/ProgressCallback.mdx) | An optional callback to keep the user informed. |

#### Returns

`Promise`\<`string`\>

The encrypted JSON wallet.

#### Source

[src.ts/wallet/wallet.ts:68](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/wallet.ts#L68)

***

### encryptSync()

```ts
encryptSync(password): string
```

Returns a [JSON Keystore Wallet](json-wallets) encryped with
 `password`.

 It is preferred to use the [async version](encrypt) instead,
 which allows a [**ProgressCallback**](/content/type-aliases/ProgressCallback.mdx) to keep the user informed.

 This method will block the event loop (freezing all UI) until
 it is complete, which may be a non-trivial duration.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `password` | `string` \| `Uint8Array` | The password to encrypt the wallet with. |

#### Returns

`string`

The encrypted JSON wallet.

#### Source

[src.ts/wallet/wallet.ts:86](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/wallet.ts#L86)

***

### signMessageSync()

```ts
signMessageSync(message): string
```

Returns the signature for `message` signed with this wallet.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `message` | `string` \| `Uint8Array` | The message to sign. |

#### Returns

`string`

The serialized signature.

#### Inherited from

[`BaseWallet`](/content/classes/BaseWallet.mdx).[`signMessageSync`](/content/classes/BaseWallet.mdx#signmessagesync)

#### Source

[src.ts/wallet/base-wallet.ts:117](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/base-wallet.ts#L117)

***

### createRandom()

```ts
static createRandom(path, provider?): QuaiHDWallet
```

Creates a new random [**QuaiHDWallet**](/content/classes/QuaiHDWallet.mdx) using the available
 [cryptographic random source](randomBytes).

 If there is no crytographic random source, this will throw.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `path` | `string` | The derivation path for the wallet. |
| `provider`? | `null` \| [`Provider`](/content/interfaces/Provider.mdx) | An optional provider to connect the wallet to. |

#### Returns

[`QuaiHDWallet`](/content/classes/QuaiHDWallet.mdx)

The new wallet.

#### Source

[src.ts/wallet/wallet.ts:172](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/wallet.ts#L172)

***

### fromEncryptedJson()

```ts
static fromEncryptedJson(
   json, 
   password, 
progress?): Promise<Wallet | QuaiHDWallet>
```

Creates (asynchronously) a **Wallet** by decrypting the `json`
 with `password`.

 If `progress` is provided, it is called periodically during
 decryption so that any UI can be updated.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `json` | `string` | The JSON data to decrypt. |
| `password` | `string` \| `Uint8Array` | The password to decrypt the JSON data. |
| `progress`? | [`ProgressCallback`](/content/type-aliases/ProgressCallback.mdx) | An optional callback to keep the user informed. |

#### Returns

`Promise`\<[`Wallet`](/content/classes/Wallet.mdx) \| [`QuaiHDWallet`](/content/classes/QuaiHDWallet.mdx)\>

The decrypted wallet.

#### Source

[src.ts/wallet/wallet.ts:123](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/wallet.ts#L123)

***

### fromEncryptedJsonSync()

```ts
static fromEncryptedJsonSync(json, password): Wallet | QuaiHDWallet
```

Creates a **Wallet** by decrypting the `json` with `password`.

 The [**fromEncryptedJson**](/content/classes/Wallet.mdx#fromencryptedjson) method is preferred, as this method
 will lock up and freeze the UI during decryption, which may take
 some time.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `json` | `string` | The JSON data to decrypt. |
| `password` | `string` \| `Uint8Array` | The password to decrypt the JSON data. |

#### Returns

[`Wallet`](/content/classes/Wallet.mdx) \| [`QuaiHDWallet`](/content/classes/QuaiHDWallet.mdx)

The decrypted wallet.

#### Source

[src.ts/wallet/wallet.ts:149](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/wallet.ts#L149)

***

### fromPhrase()

```ts
static fromPhrase(
   phrase, 
   path, 
   provider?, 
   wordlist?): QuaiHDWallet | QiHDWallet
```

"m/44'/60'/0'/0/0"
 Creates a [**QuaiHDWallet**](/content/classes/QuaiHDWallet.mdx) for `phrase`.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `phrase` | `string` | The mnemonic phrase to create the wallet with. |
| `path` | `string` | The derivation path for the wallet. |
| `provider`? | [`Provider`](/content/interfaces/Provider.mdx) | An optional provider to connect the wallet to. |
| `wordlist`? | [`Wordlist`](/content/classes/Wordlist.mdx) | An optional wordlist to use for the mnemonic phrase. |

#### Returns

[`QuaiHDWallet`](/content/classes/QuaiHDWallet.mdx) \| [`QiHDWallet`](/content/classes/QiHDWallet.mdx)

The new wallet.

#### Source

[src.ts/wallet/wallet.ts:187](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/wallet.ts#L187)
