---
title: Network
---

A **Network** provides access to a chain's properties and allows for plug-ins to extend functionality.

## Constructors

### new Network()

```ts
new Network(name, chainId): Network
```

Creates a new **Network** for `name` and `chainId`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `name` | `string` |
| `chainId` | [`BigNumberish`](/content/type-aliases/BigNumberish) |

#### Returns

[`Network`](/content/classes/Network)

#### Source

[src/providers/network.ts:43](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L43)

## Accessors

### chainId

```ts
get chainId(): bigint
```

The network chain ID.

#### Returns

`bigint`

#### Source

[src/providers/network.ts:70](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L70)

***

### name

```ts
get name(): string
```

The network common name.

This is the canonical name, as networks migh have multiple names.

#### Returns

`string`

#### Source

[src/providers/network.ts:60](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L60)

## Methods

### clone()

```ts
clone(): Network
```

Create a copy of this Network.

#### Returns

[`Network`](/content/classes/Network)

#### Source

[src/providers/network.ts:128](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L128)

***

### matches()

```ts
matches(other): boolean
```

Returns true if `other` matches this network. Any chain ID must match, and if no chain ID is present, the name
must match.

This method does not currently check for additional properties, such as plug-in compatibility.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `other` | [`Networkish`](/content/type-aliases/Networkish) | The network to compare. |

#### Returns

`boolean`

True if the networks match.

#### Source

[src/providers/network.ts:87](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L87)

***

### toJSON()

```ts
toJSON(): any
```

Returns a JSON-compatible representation of a Network.

#### Returns

`any`

#### Source

[src/providers/network.ts:51](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L51)

***

### from()

```ts
static from(network?): Network
```

Returns a new Network for the `network` name or chainId.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `network`? | [`Networkish`](/content/type-aliases/Networkish) | The network to get. |

#### Returns

[`Network`](/content/classes/Network)

The Network instance.

#### Source

[src/providers/network.ts:140](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L140)

***

### register()

```ts
static register(nameOrChainId, networkFunc): void
```

Register `nameOrChainId` with a function which returns an instance of a Network representing that chain.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `nameOrChainId` | `string` \| `number` \| `bigint` | The name or chain ID to register. |
| `networkFunc` | () => [`Network`](/content/classes/Network) | The function to create the Network. |

#### Returns

`void`

#### Throws

If a network is already registered for `nameOrChainId`.

#### Source

[src/providers/network.ts:192](https://github.com/dominant-strategies/quais-6.js/blob/95934f9b18ff90a0666dd9772179b612f2ad5510/src/providers/network.ts#L192)
