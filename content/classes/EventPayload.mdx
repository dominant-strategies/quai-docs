---
title: EventPayload
description: When an ...
---

When an [**EventEmitterable**](/content/interfaces/EventEmitterable.mdx) triggers a [[Listener]], the
 callback always ahas one additional argument passed, which is
 an **EventPayload**.

## TODO

Listener is no longer exported, need to revise comment or remove link

## Extended by

- [`ContractUnknownEventPayload`](/content/classes/ContractUnknownEventPayload.mdx)

## Type parameters

| Type parameter |
| :------ |
| `T` |

## Constructors

### new EventPayload()

```ts
new EventPayload<T>(
   emitter, 
   listener, 
filter): EventPayload<T>
```

Create a new **EventPayload** for `emitter` with
 the `listener` and for `filter`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `emitter` | [`EventEmitterable`](/content/interfaces/EventEmitterable.mdx)\<`T`\> |
| `listener` | `null` \| [`Listener`](/content/type-aliases/Listener.mdx) |
| `filter` | `T` |

#### Returns

[`EventPayload`](/content/classes/EventPayload.mdx)\<`T`\>

#### Source

[src.ts/utils/events.ts:100](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/utils/events.ts#L100)

## Properties

| Property | Modifier | Type | Description |
| :------ | :------ | :------ | :------ |
| `emitter` | `readonly` | [`EventEmitterable`](/content/interfaces/EventEmitterable.mdx)\<`T`\> | The **EventEmitterable**. |
| `filter` | `readonly` | `T` | The event filter. |

## Methods

### removeListener()

```ts
removeListener(): Promise<void>
```

Unregister the triggered listener for future events.

#### Returns

`Promise`\<`void`\>

#### Source

[src.ts/utils/events.ts:108](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/utils/events.ts#L108)
