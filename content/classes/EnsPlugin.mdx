---
title: EnsPlugin
description: An **EnsPlugin** allows a [Network](../classes/Network) to
  specify   the ENS Registry Contract address and the target network to use
  when   using that contract.   Various testnets have their own instance of the
  contract to use, but  in general, the mainnet instance supports multi-chain
  addresses and  should be used....
---

An **EnsPlugin** allows a [Network](../classes/Network) to specify 
 the ENS Registry Contract address and the target network to use when 
 using that contract.

 Various testnets have their own instance of the contract to use, but
 in general, the mainnet instance supports multi-chain addresses and
 should be used.

## Extends

- [`NetworkPlugin`](/content/classes/NetworkPlugin.mdx)

## Constructors

### new EnsPlugin()

```ts
new EnsPlugin(address?, targetNetwork?): EnsPlugin
```

Creates a new **EnsPlugin** connected to `address` on the
 `targetNetwork`. The default ENS address and mainnet is used
 if unspecified.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `address`? | `null` \| `string` |
| `targetNetwork`? | `null` \| `number` |

#### Returns

[`EnsPlugin`](/content/classes/EnsPlugin.mdx)

#### Overrides

[`NetworkPlugin`](/content/classes/NetworkPlugin.mdx).[`constructor`](/content/classes/NetworkPlugin.mdx#constructors)

#### Source

[src.ts/providers/plugins-network.ts:192](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/providers/plugins-network.ts#L192)

## Properties

| Property | Modifier | Type | Description | Inherited from |
| :------ | :------ | :------ | :------ | :------ |
| `address` | `readonly` | `string` | The ENS Registrty Contract address. | - |
| `name` | `readonly` | `string` | The name of the plugin.<br /><br /> It is recommended to use reverse-domain-notation, which permits<br /> unique names with a known authority as well as hierarchal entries. | [`NetworkPlugin`](/content/classes/NetworkPlugin.mdx).`name` |
| `targetNetwork` | `readonly` | `number` | The chain ID that the ENS contract lives on. | - |
