---
title: HDNodeVoidWallet
description: A **HDNodeVoidWallet** cannot sign, but provides access to  the
  children nodes of a
  [BIP-32](https://github.com/bitcoin/bips/blob/master/bip-0032.mediawiki) HD
  wallet addresses.   The can be created by using an extended ...
---

A **HDNodeVoidWallet** cannot sign, but provides access to
 the children nodes of a [BIP-32](https://github.com/bitcoin/bips/blob/master/bip-0032.mediawiki) HD wallet addresses.

 The can be created by using an extended `xpub` key to
 [**QuaiHDWallet.fromExtendedKey**](/content/classes/QuaiHDWallet.mdx#fromextendedkey) or by 
 [nuetering](QuaiHDWallet-neuter) a [**QuaiHDWallet**](/content/classes/QuaiHDWallet.mdx).

## Extends

- [`VoidSigner`](/content/classes/VoidSigner.mdx)

## Constructors

### new HDNodeVoidWallet()

```ts
private new HDNodeVoidWallet(
   guard, 
   address, 
   publicKey, 
   accountFingerprint, 
   chainCode, 
   path, 
   index, 
   depth, 
   provider): HDNodeVoidWallet
```

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `guard` | `any` |
| `address` | `string` |
| `publicKey` | `string` |
| `accountFingerprint` | `string` |
| `chainCode` | `string` |
| `path` | `null` \| `string` |
| `index` | `number` |
| `depth` | `number` |
| `provider` | `null` \| [`Provider`](/content/interfaces/Provider.mdx) |

#### Returns

[`HDNodeVoidWallet`](/content/classes/HDNodeVoidWallet.mdx)

#### Overrides

[`VoidSigner`](/content/classes/VoidSigner.mdx).[`constructor`](/content/classes/VoidSigner.mdx#constructors)

#### Source

[src.ts/wallet/quai-hdwallet.ts:502](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/quai-hdwallet.ts#L502)

## Properties

| Property | Modifier | Type | Description | Inherited from |
| :------ | :------ | :------ | :------ | :------ |
| `accountFingerprint` | `readonly` | `string` | The parent node fingerprint. | - |
| `address` | `readonly` | `string` | The signer address. | [`VoidSigner`](/content/classes/VoidSigner.mdx).`address` |
| `chainCode` | `readonly` | `string` | The chaincode, which is effectively a public key used<br /> to derive children. | - |
| `depth` | `readonly` | `number` | The depth of this wallet, which is the number of components<br /> in its path. | - |
| `fingerprint` | `readonly` | `string` | The fingerprint.<br /><br /> A fingerprint allows quick qay to detect parent and child nodes,<br /> but developers should be prepared to deal with collisions as it<br /> is only 4 bytes. | - |
| `index` | `readonly` | `number` | The child index of this wallet. Values over `2 *\* 31` indicate<br /> the node is hardened. | - |
| `path` | `readonly` | `null` \| `string` | The derivation path of this wallet.<br /><br /> Since extended keys do not provider full path details, this<br /> may be `null`, if instantiated from a source that does not<br /> enocde it. | - |
| `provider` | `readonly` | `null` \| [`Provider`](/content/interfaces/Provider.mdx) | The provider this signer is connected to. | [`VoidSigner`](/content/classes/VoidSigner.mdx).`provider` |
| `publicKey` | `readonly` | `string` | The compressed public key. | - |

## Accessors

### extendedKey

```ts
get extendedKey(): string
```

The extended key.

 This key will begin with the prefix `xpub` and can be used to
 reconstruct this neutered key to derive its children addresses.

#### Returns

`string`

#### Source

[src.ts/wallet/quai-hdwallet.ts:525](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/quai-hdwallet.ts#L525)

## Methods

### deriveChild()

```ts
deriveChild(_index): HDNodeVoidWallet
```

Return the child for `index`.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `_index` | [`Numeric`](/content/type-aliases/Numeric.mdx) | The index of the child to derive. |

#### Returns

[`HDNodeVoidWallet`](/content/classes/HDNodeVoidWallet.mdx)

The derived child HD Node.

#### Source

[src.ts/wallet/quai-hdwallet.ts:558](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/quai-hdwallet.ts#L558)

***

### derivePath()

```ts
derivePath(path): HDNodeVoidWallet
```

Return the signer for `path` from this node.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `path` | `string` | The path to derive. |

#### Returns

[`HDNodeVoidWallet`](/content/classes/HDNodeVoidWallet.mdx)

The derived HD Node.

#### Source

[src.ts/wallet/quai-hdwallet.ts:584](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/quai-hdwallet.ts#L584)

***

### hasPath()

```ts
hasPath(): this is Object
```

Returns true if this wallet has a path, providing a Type Guard
 that the path is non-null.

#### Returns

`this is Object`

True if the path is non-null.

#### Source

[src.ts/wallet/quai-hdwallet.ts:550](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/wallet/quai-hdwallet.ts#L550)
