---
title: FeeData
description: A **FeeData** wraps all the fee-related values associated with  the network....
---

A **FeeData** wraps all the fee-related values associated with
 the network.

## Constructors

### new FeeData()

```ts
new FeeData(
   gasPrice?, 
   maxFeePerGas?, 
   maxPriorityFeePerGas?): FeeData
```

Creates a new FeeData for `gasPrice`, `maxFeePerGas` and
 `maxPriorityFeePerGas`.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `gasPrice`? | `null` \| `bigint` |
| `maxFeePerGas`? | `null` \| `bigint` |
| `maxPriorityFeePerGas`? | `null` \| `bigint` |

#### Returns

[`FeeData`](/content/classes/FeeData.mdx)

#### Source

[src.ts/providers/provider.ts:101](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/providers/provider.ts#L101)

## Properties

| Property | Modifier | Type | Description |
| :------ | :------ | :------ | :------ |
| `gasPrice` | `readonly` | `null` \| `bigint` | The gas price for legacy networks. |
| `maxFeePerGas` | `readonly` | `null` \| `bigint` | The maximum fee to pay per gas.<br /><br /> The base fee per gas is defined by the network and based on<br /> congestion, increasing the cost during times of heavy load<br /> and lowering when less busy.<br /><br /> The actual fee per gas will be the base fee for the block<br /> and the priority fee, up to the max fee per gas.<br /><br /> This will be `null` on legacy networks (i.e. [pre-EIP-1559](https://eips.ethereum.org/EIPS/eip-1559)) |
| `maxPriorityFeePerGas` | `readonly` | `null` \| `bigint` | The additional amout to pay per gas to encourage a validator<br /> to include the transaction.<br /><br /> The purpose of this is to compensate the validator for the<br /> adjusted risk for including a given transaction.<br /><br /> This will be `null` on legacy networks (i.e. [pre-EIP-1559](https://eips.ethereum.org/EIPS/eip-1559)) |

## Methods

### toJSON()

```ts
toJSON(): any
```

Returns a JSON-friendly value.

#### Returns

`any`

#### Source

[src.ts/providers/provider.ts:112](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/providers/provider.ts#L112)
