[**quais6**](../README.md) • **Docs**

***

[quais6](../globals.md) / ContractFactory

# Class: ContractFactory\<A, I\>

A **ContractFactory** is used to deploy a Contract to the blockchain.

## Type parameters

• **A** *extends* `any`[] = `any`[]

• **I** = [`BaseContract`](BaseContract.md)

## Constructors

### new ContractFactory()

> **new ContractFactory**\<`A`, `I`\>(`abi`, `bytecode`, `runner`?): [`ContractFactory`](ContractFactory.md)\<`A`, `I`\>

Create a new **ContractFactory** with `abi` and `bytecode`,
 optionally connected to `runner`.

 The `bytecode` may be the `bytecode` property within the
 standard Solidity JSON output.

#### Parameters

• **abi**: [`Interface`](Interface.md) \| [`InterfaceAbi`](../type-aliases/InterfaceAbi.md)

• **bytecode**: [`BytesLike`](../type-aliases/BytesLike.md) \| `object`

• **runner?**: `null` \| [`ContractRunner`](../interfaces/ContractRunner.md)

#### Returns

[`ContractFactory`](ContractFactory.md)\<`A`, `I`\>

#### Source

[src.ts/contract/factory.ts:57](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L57)

## Properties

### bytecode

> `readonly` **bytecode**: `string`

The Contract deployment bytecode. Often called the initcode.

#### Source

[src.ts/contract/factory.ts:43](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L43)

***

### interface

> `readonly` **interface**: [`Interface`](Interface.md)

The Contract Interface.

#### Source

[src.ts/contract/factory.ts:38](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L38)

***

### runner

> `readonly` **runner**: `null` \| [`ContractRunner`](../interfaces/ContractRunner.md)

The ContractRunner to deploy the Contract as.

#### Source

[src.ts/contract/factory.ts:48](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L48)

## Methods

### connect()

> **connect**(`runner`): [`ContractFactory`](ContractFactory.md)\<`A`, `I`\>

Return a new **ContractFactory** with the same ABI and bytecode,
 but connected to `runner`.

#### Parameters

• **runner**: `null` \| [`ContractRunner`](../interfaces/ContractRunner.md)

The runner to connect to.

#### Returns

[`ContractFactory`](ContractFactory.md)\<`A`, `I`\>

A new ContractFactory.

#### Source

[src.ts/contract/factory.ts:239](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L239)

***

### deploy()

> **deploy**(...`args`): `Promise`\<[`BaseContract`](BaseContract.md) & `object` & `Omit`\<`I`, keyof [`BaseContract`](BaseContract.md)\>\>

Resolves to the Contract deployed by passing `args` into the
 constructor.

 This will resovle to the Contract before it has been deployed to the
 network, so the [baseContract.waitForDeployment](../classes/BaseContract#waitForDeployment) should be used before
 sending any transactions to it.

#### Parameters

• ...**args**: `ContractMethodArgs`\<`A`\>

The arguments to the constructor.

#### Returns

`Promise`\<[`BaseContract`](BaseContract.md) & `object` & `Omit`\<`I`, keyof [`BaseContract`](BaseContract.md)\>\>

A promise resolving to the Contract.

#### Source

[src.ts/contract/factory.ts:172](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L172)

***

### getDeployTransaction()

> **getDeployTransaction**(...`args`): `Promise`\<[`ContractDeployTransaction`](../interfaces/ContractDeployTransaction.md)\>

Resolves to the transaction to deploy the contract, passing `args`
 into the constructor.

#### Parameters

• ...**args**: `ContractMethodArgs`\<`A`\>

The arguments to the constructor.

#### Returns

`Promise`\<[`ContractDeployTransaction`](../interfaces/ContractDeployTransaction.md)\>

A promise resolving to the deployment transaction.

#### Source

[src.ts/contract/factory.ts:86](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L86)

***

### fromSolidity()

> `static` **fromSolidity**\<`A`, `I`\>(`output`, `runner`?): [`ContractFactory`](ContractFactory.md)\<`A`, `I`\>

Create a new **ContractFactory** from the standard Solidity JSON output.

#### Type parameters

• **A** *extends* `any`[] = `any`[]

• **I** = [`ContractInterface`](../interfaces/ContractInterface.md)

#### Parameters

• **output**: `any`

The Solidity JSON output.

• **runner?**: [`ContractRunner`](../interfaces/ContractRunner.md)

The runner to connect to.

#### Returns

[`ContractFactory`](ContractFactory.md)\<`A`, `I`\>

A new ContractFactory.

#### Source

[src.ts/contract/factory.ts:250](https://github.com/dominant-strategies/quais-6.js/blob/aca1855329c3946c627ab7f16ac5304cea7d20c5/src.ts/contract/factory.ts#L250)
