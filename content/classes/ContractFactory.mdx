---
title: ContractFactory
description: A **ContractFactory** is used to deploy a Contract to the blockchain....
---

A **ContractFactory** is used to deploy a Contract to the blockchain.

## Type parameters

| Type parameter | Value |
| :------ | :------ |
| `A` *extends* `any`[] | `any`[] |
| `I` | [`BaseContract`](/content/classes/BaseContract.mdx) |

## Constructors

### new ContractFactory()

```ts
new ContractFactory<A, I>(
   abi, 
   bytecode, 
runner?): ContractFactory<A, I>
```

Create a new **ContractFactory** with `abi` and `bytecode`,
 optionally connected to `runner`.

 The `bytecode` may be the `bytecode` property within the
 standard Solidity JSON output.

#### Parameters

| Parameter | Type |
| :------ | :------ |
| `abi` | [`Interface`](/content/classes/Interface.mdx) \| [`InterfaceAbi`](/content/type-aliases/InterfaceAbi.mdx) |
| `bytecode` | [`BytesLike`](/content/type-aliases/BytesLike.mdx) \| \{ `"object"`: `string`; \} |
| `runner`? | `null` \| [`ContractRunner`](/content/interfaces/ContractRunner.mdx) |

#### Returns

[`ContractFactory`](/content/classes/ContractFactory.mdx)\<`A`, `I`\>

#### Source

[src.ts/contract/factory.ts:57](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/contract/factory.ts#L57)

## Properties

| Property | Modifier | Type | Description |
| :------ | :------ | :------ | :------ |
| `bytecode` | `readonly` | `string` | The Contract deployment bytecode. Often called the initcode. |
| `interface` | `readonly` | [`Interface`](/content/classes/Interface.mdx) | The Contract Interface. |
| `runner` | `readonly` | `null` \| [`ContractRunner`](/content/interfaces/ContractRunner.mdx) | The ContractRunner to deploy the Contract as. |

## Methods

### connect()

```ts
connect(runner): ContractFactory<A, I>
```

Return a new **ContractFactory** with the same ABI and bytecode,
 but connected to `runner`.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `runner` | `null` \| [`ContractRunner`](/content/interfaces/ContractRunner.mdx) | The runner to connect to. |

#### Returns

[`ContractFactory`](/content/classes/ContractFactory.mdx)\<`A`, `I`\>

A new ContractFactory.

#### Source

[src.ts/contract/factory.ts:239](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/contract/factory.ts#L239)

***

### deploy()

```ts
deploy(...args): Promise<BaseContract & {
  "deploymentTransaction": ContractTransactionResponse;
} & Omit<I, keyof BaseContract>>
```

Resolves to the Contract deployed by passing `args` into the
 constructor.

 This will resovle to the Contract before it has been deployed to the
 network, so the [baseContract.waitForDeployment](../classes/BaseContract#waitForDeployment) should be used before
 sending any transactions to it.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| ...`args` | `ContractMethodArgs`\<`A`\> | The arguments to the constructor. |

#### Returns

`Promise`\<[`BaseContract`](/content/classes/BaseContract.mdx) & \{
  `"deploymentTransaction"`: [`ContractTransactionResponse`](/content/classes/ContractTransactionResponse.mdx);
 \} & `Omit`\<`I`, keyof [`BaseContract`](/content/classes/BaseContract.mdx)\>\>

A promise resolving to the Contract.

#### Source

[src.ts/contract/factory.ts:172](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/contract/factory.ts#L172)

***

### getDeployTransaction()

```ts
getDeployTransaction(...args): Promise<ContractDeployTransaction>
```

Resolves to the transaction to deploy the contract, passing `args`
 into the constructor.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| ...`args` | `ContractMethodArgs`\<`A`\> | The arguments to the constructor. |

#### Returns

`Promise`\<[`ContractDeployTransaction`](/content/interfaces/ContractDeployTransaction.mdx)\>

A promise resolving to the deployment transaction.

#### Source

[src.ts/contract/factory.ts:86](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/contract/factory.ts#L86)

***

### fromSolidity()

```ts
static fromSolidity<A, I>(output, runner?): ContractFactory<A, I>
```

Create a new **ContractFactory** from the standard Solidity JSON output.

#### Type parameters

| Type parameter | Value |
| :------ | :------ |
| `A` *extends* `any`[] | `any`[] |
| `I` | [`ContractInterface`](/content/interfaces/ContractInterface.mdx) |

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `output` | `any` | The Solidity JSON output. |
| `runner`? | [`ContractRunner`](/content/interfaces/ContractRunner.mdx) | The runner to connect to. |

#### Returns

[`ContractFactory`](/content/classes/ContractFactory.mdx)\<`A`, `I`\>

A new ContractFactory.

#### Source

[src.ts/contract/factory.ts:250](https://github.com/dominant-strategies/quais-6.js/blob/3f482dd134064d41657ff7dc9ba8162f741c9c83/src.ts/contract/factory.ts#L250)
