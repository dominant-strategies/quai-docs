---
title: FewestCoinSelector
icon: square-c
iconType: solid
---

The FewestCoinSelector class provides a coin selection algorithm that selects the fewest UTXOs required to meet the
target amount. This algorithm is useful for minimizing the size of the transaction and the fees associated with it.

This class is a sub-class of AbstractCoinSelector | **AbstractCoinSelector** and implements the
AbstractCoinSelector.performSelection | **performSelection** method to provide the actual coin selection
logic.

## Extends

- `AbstractCoinSelector`

## Constructors

### new FewestCoinSelector()

```ts
new FewestCoinSelector(availableUTXOs): FewestCoinSelector
```

Constructs a new AbstractCoinSelector instance with an empty UTXO array.

#### Parameters

| Parameter | Type | Default value |
| :------ | :------ | :------ |
| `availableUTXOs` | [`UTXO`](/sdk/content/classes/UTXO)[] | `[]` |

#### Returns

[`FewestCoinSelector`](/sdk/content/classes/FewestCoinSelector)

#### Inherited from

`AbstractCoinSelector.constructor`

#### Source

[transaction/abstract-coinselector.ts:108](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/abstract-coinselector.ts#L108)

## Accessors

### availableUTXOs

```ts
get availableUTXOs(): UTXO[]
```

Gets the available UTXOs.

```ts
set availableUTXOs(value): void
```

Sets the available UTXOs.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `value` | `UTXOLike`[] | The UTXOs to set. |

#### Returns

[`UTXO`](/sdk/content/classes/UTXO)[]

The available UTXOs.

#### Source

[transaction/abstract-coinselector.ts:50](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/abstract-coinselector.ts#L50)

***

### changeOutputs

```ts
get changeOutputs(): UTXO[]
```

Gets the change outputs.

```ts
set changeOutputs(value): void
```

Sets the change outputs.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `value` | `UTXOLike`[] | The change outputs to set. |

#### Returns

[`UTXO`](/sdk/content/classes/UTXO)[]

The change outputs.

#### Source

[transaction/abstract-coinselector.ts:90](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/abstract-coinselector.ts#L90)

***

### spendOutputs

```ts
get spendOutputs(): UTXO[]
```

Gets the spend outputs.

```ts
set spendOutputs(value): void
```

Sets the spend outputs.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `value` | `UTXOLike`[] | The spend outputs to set. |

#### Returns

[`UTXO`](/sdk/content/classes/UTXO)[]

The spend outputs.

#### Source

[transaction/abstract-coinselector.ts:72](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/abstract-coinselector.ts#L72)

## Methods

### performSelection()

```ts
performSelection(target): SelectedCoinsResult
```

The largest first coin selection algorithm.

This algorithm selects the largest UTXOs first, and continues to select UTXOs until the target amount is reached.
If the total value of the selected UTXOs is greater than the target amount, the remaining value is returned as a
change output.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `target` | `SpendTarget` | The target amount to spend. |

#### Returns

`SelectedCoinsResult`

The selected UTXOs and change outputs.

#### Overrides

`AbstractCoinSelector.performSelection`

#### Source

[transaction/coinselector-fewest.ts:26](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/coinselector-fewest.ts#L26)

***

### sortUTXOsByDenomination()

```ts
private sortUTXOsByDenomination(utxos, direction): UTXO[]
```

Sorts UTXOs by their denomination.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `utxos` | [`UTXO`](/sdk/content/classes/UTXO)[] | The UTXOs to sort. |
| `direction` | `"asc"` \| `"desc"` | The direction to sort ('asc' for ascending, 'desc' for descending). |

#### Returns

[`UTXO`](/sdk/content/classes/UTXO)[]

The sorted UTXOs.

#### Source

[transaction/coinselector-fewest.ts:153](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/coinselector-fewest.ts#L153)

***

### validateTarget()

```ts
private validateTarget(target): void
```

Validates the target amount.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `target` | `SpendTarget` | The target amount to validate. |

#### Returns

`void`

#### Throws

Will throw an error if the target amount is less than or equal to 0.

#### Source

[transaction/coinselector-fewest.ts:176](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/coinselector-fewest.ts#L176)

***

### validateUTXOs()

```ts
private validateUTXOs(): void
```

Validates the available UTXOs.

#### Returns

`void`

#### Throws

Will throw an error if there are no available UTXOs.

#### Source

[transaction/coinselector-fewest.ts:187](https://github.com/dominant-strategies/quais.js/blob/c523dd62336b397db22e917054b8a36177cabf46/src/transaction/coinselector-fewest.ts#L187)
