---
title: keccak256
icon: square-f
iconType: solid
---

```ts
function keccak256(_data): string
```

Compute the cryptographic KECCAK256 hash of `data`.

The `data` **must** be a data representation, to compute the hash of UTF-8 data use the [**id**}(../functions/id)
function.

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `_data` | [`BytesLike`](/sdk/content/type-aliases/BytesLike) | The data to hash. |

## Returns

`string`

DataHexstring

The hash of the data.

## Example

```ts
keccak256('0x');

keccak256('0x1337');

keccak256(new Uint8Array([0x13, 0x37]));

// Strings are assumed to be DataHexString, otherwise it will
// throw. To hash UTF-8 data, see the note above.
keccak256('Hello World');
```

## Source

[crypto/keccak.ts:44](https://github.com/dominant-strategies/quais.js/blob/35fe6504b04c7e2a611071dcdeb2930773768d6d/src/crypto/keccak.ts#L44)
